# Fake Chat Screenshot Generator

Веб-приложение для создания реалистичных скриншотов переписок в популярных мессенджерах.

## Возможности

- 📱 **Поддержка 4 мессенджеров**: Telegram, WhatsApp, Viber, VK
- 🎨 **Реалистичный дизайн**: Точная стилизация под каждый мессенджер
- ⚡ **Live Preview**: Мгновенное отображение изменений
- 📝 **Гибкая настройка**: Редактирование текста, времени, отправителя
- 💾 **Экспорт в PNG**: Скачивание готовых изображений высокого качества
- 🔄 **Множественные скриншоты**: Автоматическое разделение на несколько изображений при большом количестве сообщений
- 🌐 **API**: Возможность генерации через POST запросы

## Технологии

- **Next.js 15** с App Router
- **React 19**
- **TypeScript**
- **Tailwind CSS**
- **shadcn/ui** компоненты
- **html2canvas** для экспорта изображений

## Установка и запуск

### Локально

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd FakeChatScreenshot
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите в режиме разработки:
```bash
npm run dev
```

4. Откройте [http://localhost:3000](http://localhost:3000) в браузере

### Production build

```bash
npm run build
npm start
```

## Деплой на Vercel

### Автоматический деплой

1. Установите Vercel CLI (если еще не установлен):
```bash
npm i -g vercel
```

2. Деплой:
```bash
vercel
```

### Через GitHub

1. Отправьте код в GitHub репозиторий
2. Зайдите на [vercel.com](https://vercel.com)
3. Импортируйте ваш GitHub репозиторий
4. Vercel автоматически определит Next.js проект и настроит деплой

### Конфигурация для Vercel

Проект уже настроен и готов к деплою на Vercel. Все необходимые конфигурационные файлы присутствуют:
- `next.config.ts` - конфигурация Next.js
- `vercel.json` - конфигурация Vercel (опционально)

## API

### GET /api/generate

Информация о API

**Response:**
```json
{
  "message": "Fake Chat Screenshot Generator API",
  "version": "1.0.0",
  "endpoints": { ... }
}
```

### POST /api/generate

Генерация конфигурации чата для создания скриншотов

**Request Body:**
```json
{
  "messenger": "telegram",
  "contactName": "John Doe",
  "contactAvatar": "https://example.com/avatar.jpg",
  "messages": [
    {
      "id": "1",
      "text": "Привет!",
      "sender": "contact",
      "timestamp": "12:30"
    },
    {
      "id": "2",
      "text": "Привет! Как дела?",
      "sender": "user",
      "timestamp": "12:31"
    }
  ]
}
```

**Response:**
```json
{
  "success": true,
  "message": "Configuration received",
  "config": { ... }
}
```

### Пример использования API

```javascript
const response = await fetch('/api/generate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    messenger: 'telegram',
    contactName: 'John Doe',
    messages: [
      { id: '1', text: 'Hello!', sender: 'contact', timestamp: '12:00' },
      { id: '2', text: 'Hi there!', sender: 'user', timestamp: '12:01' }
    ]
  })
});

const data = await response.json();
console.log(data);
```

## Использование

1. **Выберите мессенджер** из выпадающего списка
2. **Настройте собеседника**: введите имя и опционально URL аватарки
3. **Укажите количество сообщений**
4. **Заполните сообщения**: для каждого сообщения укажите:
   - Отправителя (Вы или Собеседник)
   - Текст сообщения
   - Время (опционально)
5. **Просмотрите результат** в правой панели
6. **Нажмите "Сохранить изображения"** для экспорта

## Структура проекта

```
├── app/
│   ├── api/
│   │   └── generate/          # API endpoints
│   ├── globals.css            # Глобальные стили
│   ├── layout.tsx             # Layout приложения
│   └── page.tsx               # Главная страница
├── components/
│   ├── messenger-themes/      # Компоненты тем мессенджеров
│   │   ├── TelegramChat.tsx
│   │   ├── WhatsAppChat.tsx
│   │   ├── ViberChat.tsx
│   │   └── VKChat.tsx
│   ├── ui/                    # UI компоненты shadcn/ui
│   └── ChatEditor.tsx         # Главный редактор
├── lib/
│   └── utils.ts               # Утилиты
├── types/
│   └── index.ts               # TypeScript типы
└── ...config files
```

## Особенности реализации

### Адаптивность
Все компоненты мессенджеров адаптированы под мобильные устройства и имеют максимальную ширину 400px для имитации экрана телефона.

### Экспорт изображений
При большом количестве сообщений (>10) автоматически создается несколько скриншотов, разделенных по 10 сообщений каждый.

### Темы мессенджеров
Каждый мессенджер имеет уникальную цветовую схему и стилистику:
- **Telegram**: Темная тема с фиолетовыми акцентами
- **WhatsApp**: Темная тема с зеленым для исходящих сообщений
- **Viber**: Светлая тема с фиолетовым хедером
- **VK**: Светлая тема с синим акцентом

## Лицензия

MIT

## Автор

Created with ❤️
