# Примеры использования

## Быстрый старт

### 1. Локальный запуск

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd FakeChatScreenshot

# Установите зависимости
npm install

# Запустите в режиме разработки
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 2. Production build

```bash
npm run build
npm start
```

## Использование веб-интерфейса

### Создание простого чата

1. Откройте приложение
2. Выберите мессенджер (например, Telegram)
3. Введите имя собеседника: "Анна"
4. Добавьте сообщения:
   - Сообщение 1: От собеседника - "Привет! Как дела?"
   - Сообщение 2: От вас - "Отлично, спасибо!"
5. Нажмите "Сохранить изображения"

### Создание чата с аватаркой

1. Выберите мессенджер
2. Введите имя: "John Doe"
3. Укажите URL аватарки: `https://i.pravatar.cc/150?img=1`
4. Добавьте сообщения
5. Сохраните

### Создание длинной переписки

Если вы укажете более 10 сообщений, приложение автоматически создаст несколько скриншотов:
- 1-10 сообщений → 1 изображение
- 11-20 сообщений → 2 изображения
- и т.д.

## Примеры для разных мессенджеров

### Telegram
```
Мессенджер: Telegram
Имя: @username
Стиль: Темная тема с фиолетовыми акцентами
Особенности: Скругленные углы сообщений
```

### WhatsApp
```
Мессенджер: WhatsApp
Имя: +7 900 123-45-67
Стиль: Темная тема с зеленым для ваших сообщений
Особенности: Паттерн на фоне
```

### Viber
```
Мессенджер: Viber
Имя: Контакт
Стиль: Светлая тема с фиолетовым хедером
Особенности: Градиентный фон чата
```

### VK
```
Мессенджер: VK
Имя: Пользователь ВК
Стиль: Светлая тема с синими акцентами
Особенности: Круглые аватарки у сообщений собеседника
```

## Использование API

### GET /api/generate

Получить информацию о API:

```bash
curl http://localhost:3000/api/generate
```

**Ответ:**
```json
{
  "message": "Fake Chat Screenshot Generator API",
  "version": "1.0.0",
  "endpoints": { ... }
}
```

### POST /api/generate

Отправить конфигурацию чата:

```bash
curl -X POST http://localhost:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "messenger": "telegram",
    "contactName": "John Doe",
    "messages": [
      {
        "id": "1",
        "text": "Привет!",
        "sender": "contact",
        "timestamp": "12:30"
      },
      {
        "id": "2",
        "text": "Привет! Как дела?",
        "sender": "user",
        "timestamp": "12:31"
      }
    ]
  }'
```

**Ответ:**
```json
{
  "success": true,
  "message": "Configuration received. Use client-side rendering to generate images.",
  "config": { ... }
}
```

### JavaScript пример

```javascript
async function generateChat() {
  const config = {
    messenger: "whatsapp",
    contactName: "Alice",
    contactAvatar: "https://i.pravatar.cc/150?img=5",
    messages: [
      {
        id: "1",
        text: "Hey! How are you?",
        sender: "contact",
        timestamp: "10:30"
      },
      {
        id: "2",
        text: "I'm good, thanks! 😊",
        sender: "user",
        timestamp: "10:31"
      },
      {
        id: "3",
        text: "Want to grab coffee later?",
        sender: "contact",
        timestamp: "10:32"
      },
      {
        id: "4",
        text: "Sure! What time?",
        sender: "user",
        timestamp: "10:33"
      }
    ]
  };

  const response = await fetch('/api/generate', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(config)
  });

  const data = await response.json();
  console.log(data);
}

generateChat();
```

### Python пример

```python
import requests
import json

def generate_chat():
    config = {
        "messenger": "vk",
        "contactName": "Мария Иванова",
        "messages": [
            {
                "id": "1",
                "text": "Привет! Как прошли выходные?",
                "sender": "contact",
                "timestamp": "15:20"
            },
            {
                "id": "2",
                "text": "Отлично! Ездили на природу 🌲",
                "sender": "user",
                "timestamp": "15:22"
            },
            {
                "id": "3",
                "text": "Круто! Фото есть?",
                "sender": "contact",
                "timestamp": "15:23"
            }
        ]
    }

    response = requests.post(
        'http://localhost:3000/api/generate',
        headers={'Content-Type': 'application/json'},
        data=json.dumps(config)
    )

    print(response.json())

if __name__ == "__main__":
    generate_chat()
```

## Продвинутые примеры

### Чат с эмодзи

```json
{
  "messenger": "telegram",
  "contactName": "Друг",
  "messages": [
    {
      "id": "1",
      "text": "Завтра встречаемся? 🤔",
      "sender": "contact",
      "timestamp": "18:00"
    },
    {
      "id": "2",
      "text": "Да, конечно! 👍",
      "sender": "user",
      "timestamp": "18:02"
    },
    {
      "id": "3",
      "text": "Отлично! До завтра! 👋",
      "sender": "contact",
      "timestamp": "18:03"
    }
  ]
}
```

### Длинное сообщение

```json
{
  "messenger": "whatsapp",
  "contactName": "Коллега",
  "messages": [
    {
      "id": "1",
      "text": "Привет! Хотел обсудить проект.\n\nУ меня есть несколько идей:\n1. Добавить новую функцию\n2. Улучшить дизайн\n3. Оптимизировать производительность\n\nЧто думаешь?",
      "sender": "contact",
      "timestamp": "09:15"
    },
    {
      "id": "2",
      "text": "Звучит интересно! Давай обсудим это на встрече.",
      "sender": "user",
      "timestamp": "09:20"
    }
  ]
}
```

### Деловая переписка

```json
{
  "messenger": "viber",
  "contactName": "Менеджер",
  "messages": [
    {
      "id": "1",
      "text": "Добрый день! Когда будет готов отчет?",
      "sender": "contact",
      "timestamp": "14:30"
    },
    {
      "id": "2",
      "text": "Здравствуйте! Отчет будет готов к концу дня.",
      "sender": "user",
      "timestamp": "14:35"
    },
    {
      "id": "3",
      "text": "Отлично, спасибо!",
      "sender": "contact",
      "timestamp": "14:36"
    }
  ]
}
```

## Советы по использованию

1. **Реалистичность**: Используйте естественное время между сообщениями (например, 12:30, 12:31, 12:33)
2. **Форматирование**: Используйте переносы строк `\n` для многострочных сообщений
3. **Эмодзи**: Добавляйте эмодзи для большей реалистичности
4. **Длина**: Старайтесь не делать сообщения слишком длинными
5. **Аватарки**: Используйте квадратные изображения для лучшего отображения

## Деплой на Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Войдите в Vercel:
```bash
vercel login
```

3. Деплой:
```bash
vercel
```

4. Production деплой:
```bash
vercel --prod
```

## Переменные окружения

В данный момент приложение не требует переменных окружения, но вы можете добавить их для:
- Ограничения API (rate limiting)
- Аналитики
- Аутентификации

Создайте файл `.env.local`:
```env
# Пример
NEXT_PUBLIC_API_URL=https://your-domain.com
```

## Часто задаваемые вопросы

**Q: Можно ли использовать локальные изображения для аватарок?**
A: Да, но в веб-приложении нужно указывать URL. Для локальных изображений разместите их в папке `public/` и используйте путь `/image.jpg`.

**Q: Какой максимальный размер изображения?**
A: Изображения экспортируются в масштабе 2x для лучшего качества. Размер зависит от количества сообщений.

**Q: Можно ли изменить цветовую схему мессенджеров?**
A: Да, отредактируйте соответствующие компоненты в `components/messenger-themes/`.

**Q: Работает ли на мобильных устройствах?**
A: Да, интерфейс полностью адаптивен и работает на всех устройствах.
